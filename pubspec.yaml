name: trypoapp
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dartz: 0.10.0-nullsafety.2
  get_it: ^7.1.3
  google_fonts: ^2.1.0
  flutter_speed_dial: ^3.0.5
  flutter_native_splash: ^1.1.8 #Versión compatible con flutter_launcher_icons 0.9.0
  flutter_launcher_icons: ^0.9.0
  collection: ^1.15.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/json/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/splash.png"
  remove_alpha_ios: true
